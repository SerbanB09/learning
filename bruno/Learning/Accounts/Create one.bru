meta {
  name: Create one
  type: http
  seq: 1
}

put {
  url: {{host}}/accounts
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
