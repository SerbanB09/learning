meta {
  name: Login
  type: http
  seq: 6
}

post {
  url: {{host}}/users/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "andrei.tomita@gmail.com",
    "password": "tomizz123"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setEnvVar("token", res.body.token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
